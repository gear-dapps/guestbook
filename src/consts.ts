const NODE_ADDRESS = process.env.REACT_APP_NODE_ADDRESS as string;
const CONTRACT_ADDRESS = process.env.REACT_APP_CONTRACT_ADDRESS as `0x${string}`;

const LOCAL_STORAGE = {
  ACCOUNT: 'account',
};

export { NODE_ADDRESS, CONTRACT_ADDRESS, LOCAL_STORAGE };
